# GUI
PySide6>=6.6,<7.0

# Downloader — keep loose, YouTube breaks old versions
yt-dlp>=2024.1.1

# NOTE: ffmpeg is NOT a pip package.
# Install via your system package manager:
#   Debian/Ubuntu/Mint:  sudo apt install ffmpeg
#   Arch:                sudo pacman -S ffmpeg
#   Fedora:              sudo dnf install ffmpeg
